<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Base.Light">
        <!-- Customize your theme here. -->
    </style>

    <style name="ButtonNoDefaultPadding">
        <item name="android:minWidth">0dp</item> <item name="android:minHeight">0dp</item>
    </style>

</resources>
